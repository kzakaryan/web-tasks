package com.bobocode.annotation;

/**
 * Annotation that can be placed on configuration classes to import {@link StringTrimmingConfiguration}.
 */
public @interface EnableStringTrimming {
    // This annotation is used to activate the String trimming functionality in the application context.
}
